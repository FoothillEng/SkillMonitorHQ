import Title from '@/components/Title';

const TeacherIndex = (props) => {
    return (
        <div className="flex w-screen flex-col items-center text-center font-oxygen text-green">
            <Title title="Teacher Home" />
        </div>
    );
};

export default TeacherIndex;
